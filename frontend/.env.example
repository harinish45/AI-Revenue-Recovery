# RecoverAI frontend
# Local FastAPI backend (no trailing /api - api.js already prefixes /api paths)
VITE_API_BASE=http://localhost:8000
# Only needed when the backend has API_KEYS configured (e.g. production).
# Leave blank for the open demo experience.
VITE_API_KEY=
